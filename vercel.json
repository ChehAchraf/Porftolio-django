{
    "version": 2,
    "installCommand": "python3 -m pip install --upgrade pip && python3 -m pip install -r requirements.txt",
    "buildCommand": "python3 manage.py collectstatic --noinput",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "portfolio/wsgi.py"
        }
    ]
}
